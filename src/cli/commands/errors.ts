// errors.ts — re-export shim (SPEC-828)
// Moved to src/cli/debug/errors.ts. This file exists only for import-path compatibility
// during the transition. Import from ../debug/errors.ts going forward.
export { runErrors } from '../debug/errors.ts';
